{
  "name": "project-athlete-360-backend",
  "version": "1.0.0",
  "description": "Enterprise backend for Project Athlete 360 — scalable API, distributed jobs, observability, and AI-integrated services.",
  "type": "module",
  "main": "dist/server.js",
  "private": true,

  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "npm run clean && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write .",
    "test": "jest --passWithNoTests",
    "clean": "rimraf dist",
    "check": "npm run lint && npm run test",
    "prepare": "husky install",
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate deploy",
    "postinstall": "npx prisma generate"
  },

  "dependencies": {
    "@prisma/client": "^6.19.0",
    "express": "^4.21.1",
    "bullmq": "^5.5.0",
    "ioredis": "^5.4.1",
    "prom-client": "^15.1.1",
    "winston": "^3.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "helmet": "^7.2.0",
    "compression": "^1.7.5",
    "pino": "^9.4.0",
    "axios": "^1.7.2",
    "zod": "^3.23.8",
    "clsx": "^2.1.1",
    "dayjs": "^1.11.11"
  },

  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.7.4",
    "@types/express": "^4.17.21",
    "ts-node-dev": "^2.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.4",
    "rimraf": "^6.0.1",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.10"
  },

  "prisma": {
    "engines": {
      "queryEngineType": "binary"
    }
  },

  "lint-staged": {
    "*.ts": ["eslint --fix", "prettier --write"]
  },

  "engines": {
    "node": "20.x"
  },

  "license": "MIT",
  "author": {
    "name": "H.K. Vaswan",
    "organization": "Project Athlete 360",
    "url": "https://projectathlete360.com"
  }
}
